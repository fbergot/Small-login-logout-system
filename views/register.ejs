<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>S'enregister</title>
</head>
<body>

    <h1>Page s'enregister</h1>
  
    <form action="/register" method="POST">
        <div>
            <label for="name">Name</label>
            <input type="text" id="name" name="name" required>
            <% if (locals.messageNotValidName) { %> 
                <p><%= locals.messageNotValidName %></p>    
            <% } %>
        </div>
        <div>
            <label for="email">Email</label>
            <input type="email" id="email" name="email" required>
            <% if (locals.messageNotValidEmail) { %> 
                <p><%= locals.messageNotValidEmail %></p>    
            <% } %>
        </div>
        <div>
            <label for="password">Password</label>
            <input type="password" id="password" name="password" required>
            <% if (locals.messageNotValidPassword) { %> 
                <p><%= locals.messageNotValidPassword %></p>    
            <% } %>
        </div>
        <button type="submit">Enregistrer</button>

    </form>
    <a href="/login">Se loguer</a>   
</body>
</html>